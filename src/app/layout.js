import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "Aaditya Rajbhar | Portfolio | Professional Flutter Developer",
  description: "Portfolio of Aaditya Rajbhar, a Flutter Developer with 2.5+ years of experience building and deploying scalable cross-platform mobile apps for Android and iOS, with 10+ live apps and 50K+ downloads.",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en" className={`${geistSans.variable} ${geistMono.variable}`}>
      <body>{children}</body>
    </html>
  );
}
